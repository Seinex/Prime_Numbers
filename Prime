#A working function to generate prime numbers from 0 to n with asymptotic analysis
#Seinex
def prime_numbers(number):
    list=[]
    if number < 2:
        return "sorry but only positive numbers allowed."
    for i in range(2,number+1):
        if i>1:
            for x in range(2,i):
                if(i%x)==0:
                    break
            else:
                list.append(i)
    return list
    
    
    
    
    
  
